import asyncio; from src.services.n8n_service import N8nService; import os; async def test_workflow(): service = N8nService(api_url=os.getenv("N8N_API_URL"), api_key=os.getenv("N8N_API_KEY")); workflow = {"name": "Test Workflow", "nodes": [], "connections": {}}; try: result = await service.create_workflow(workflow); print("Created workflow:", result); except Exception as e: print("Error:", str(e)); asyncio.run(test_workflow())
